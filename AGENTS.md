## Objective
- Développer CoursConnect (marketplace de cours particuliers, Maroc) : backend Java 21/Jakarta EE 10 + JPA/Hibernate + MySQL 8 + WildFly 33, frontend React+TypeScript+Vite.
- En cours : **dashboard professeur routé + notifications fiables livré et vérifié (backend E2E 27/27, frontend headless 157/157)** : le professeur reçoit désormais bien une notification quand l'élève annule (source de vérité backend, anti-doublon, `referenceId` qui mène à la réservation), le point « professeur annule → notification étudiant » existe (`PUT /bookings/{id}/professor-cancel`), et le dashboard est découpé en 7 pages routées (/professor, reservations, reservations/:id, notifications, profile, offers+new+edit, availability) avec sidebar et badge unread ; il reste : vérification visuelle utilisateur aux breakpoints. Le dossier `AGENTS.md` documente par ailleurs : paiement séparé du statut, lieu privé révélé après confirmation, montant calculé par le backend, cause racine encodage, favoris API-backés, photo de profil (upload/validation/suppression), réunion en ligne (plateforme publique, lien/instructions privés jusqu'à ACCEPTED), **modèle « lien de séance = réservation » (https strict, ajout après acceptation)**, **le nouveau découpage dashboard professeur + notifications avec lien vers réservation** et **la préparation production : compte admin env-driven + déploiement frontend Vercel**.

## Important Details
- **Compte admin + déploiement Vercel (préparation production, livré côté code, vérifié E2E)** : admin **uniquement** provisionné par `AdminBootstrap` (`@Startup @Singleton` EJB auto-découvert, PAS dans `getClasses()` car non-JAX-RS) à partir des env `ADMIN_EMAIL`/`ADMIN_PASSWORD` (docker-compose `wildfly.environment` ← `.env` racine gitignoré) : **idempotent** (par email → crée si absent et seulement si ADMIN_PASSWORD fourni, sinon garanti `role=ADMIN`+`enabled`, hash mis à jour si env fourni), **plus aucun mot de passe par défaut** (l'admin `admin@coursconnect.fr` a été retiré de `seed-data.sql` ; compte réel fusionné `id=1 → weborax.dev@gmail.com`) ; email **normalisé trim+lowercase** (`setEmail` des DTO Login/Register + `AuthService.normalizeEmail`) — vérifié login `"  WEBORAX.DEV@GMAIL.COM "` → 200 ; **bug logout corrigé** : `UserRepository.invalidateSession/invalidateAllSessions` appelaient `createQuery(..., UserSession.class)` sur un DELETE → Hibernate 6 `IllegalQueryOperationException` (500) — fix = `createQuery` SANS type de résultat (vérifié : logout 200 + token → 401 ensuite) ; **CORS multi-origines** : `CORS_ALLOWED_ORIGINS` env liste virgulée, jamais `*` avec credentials — `CorsConfig.resolveOrigin` écho l'origine uniquement si autorisée ; **frontend prod-prêt** : `api.ts` `API_BASE = VITE_API_URL || '/api'` (export `getApiBase()`, utilisé aussi par `AuthContext` logout), `frontend/vercel.json` (buildCommand `npm run build`, outputDirectory `dist`, rewrite SPA `/((?!api/|assets/|vite.svg).*)` → `/index.html` — pas de rewrite `/api` car l'URL backend de prod n'est volontairement PAS inventée), `frontend/.env.example` (`VITE_API_URL=` seule variable publique), `.gitignore` `.env.*` + `!.env.example` (vérifié `.env` ignoré). ⚠ **Déploiement Vercel réel non fait** : nécessite token/connexion Vercel ET URL HTTPS réelle du backend production (ne pas inventer).
- Contraintes utilisateur : pas de modification inutile du frontend validé ; ne pas réécrire l'architecture existante ; pas de fonctionnalités fictives ; étape par étape ; recompiler après chaque correction ; distinguer problème d'environnement vs problème de code.
- **Décision utilisateur : WildFly sur hôte 8081** (le 8080 = conteneur `tomcat_server` Cabinet_Medical — ne pas toucher ; Postgres 5432 = autre projet).
- **Encodage UTF-8 — cause racine trouvée et corrigée** : (1) données DB double-encodées en mojibake (`MathÃ©matiques`) importées via un client corrompu — réparées par `CONVERT(CAST(CONVERT(col USING latin1) AS BINARY) USING utf8mb4)` (colonnes pure mojibake) + UPDATEs manuels (notifications ids 1-7/11-16, reviews 1/2/4) via `backend/repair-utf8.sql` ; (2) connexion JDBC en latin1 → les écritures app corrompaient — datasource URL passée à `...?useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8` (appliquée via `backend/ds-fix.cli` + `:reload`). Tables en utf8mb4. Vérifié : écriture/lecture accents propres (ex. réservation étudiant → colonne `meeting_location` propre en base).
- **Paiement & lieu — règles métier (backend autoritaire)** : nouveaux enums `PaymentMethod` (CASH/ONLINE) + `PaymentStatus` (UNPAID/PENDING/PAID/FAILED/REFUNDED) ; **seul CASH est accepté à la création** (ONLINE → ConflictException 409 « Paiement en ligne — bientôt disponible ») ; **montant = (negotiatedPrice ?? price) × durationMinutes / 60** arrondi HALF_UP 2 déc., devise `DH`, recalculé par le backend même pour les anciennes lignes (`amountOf` fallback) ; `UNPAID` à la création, `markPaid` (professeur) uniquement si **COMPLETED** → `PAID` + `paidAt`. **Lieu privé** : `meetingLocation`/`meetingLink` exposés dans le DTO **uniquement si statut ACCEPTED ou COMPLETED** (rien sur les fiches publiques — vérifié aucune fuite). Logique par `LocationType` : STUDENT_HOME/OTHER → adresse requise à la création (409 si absente) ; PROFESSOR_HOME → snapshot `professors.teaching_address` (surchargable à l'acceptation) ; ONLINE → **lien de séance géré APRÈS acceptation** (voir modèle lien-par-réservation plus bas). `BookingAcceptDTO` (meetingLocation seul désormais — le lien ONLINE a migré vers `PUT /bookings/{id}/meeting`) ; endpoint `PUT /bookings/{id}/payment/mark-paid`.
- **Photo de profil professeur + réunion en ligne (livré, vérifié E2E+headless 40/40)** : upload `PUT /api/uploads/profiles/me` (corps brut octet-stream/image, magic bytes + taille ≤5 Mo + types JPG/PNG/WEBP via `PhotoValidator`), stockage disque `{jboss.server.data.dir}/uploads/profiles/prof-<professorId>.<ext>`, URL logique `profile_photo` ; `DELETE` supprime le fichier + colonne ; GET `/uploads/profiles/{fileName}` public (lot sûr `prof-\d+\.(jpg|png|webp)` — traversal 404), PUT/DELETE rôle PROFESSSEUR uniquement ; Avatar frontend = photo affichée sinon initiales, `onError` → initiales (jamais d'image cassée). **Réunion en ligne — MODÈLE « LIEN PAR RÉSERVATION » (refonte, headless 102/102)** : `Offer` ne porte plus QUE la `meetingPlatform` préférée (publique, non sensible) ; **le lien (`meetingLink`) + instructions sont rattachés à la réservation** via `PUT/DELETE /bookings/{id}/meeting` (rôle PROFESSOR, uniquement ACCEPTED/COMPLETED) — **https strict** (`UrlValidator.isHttpsUrl`, refuse http:// javascript: etc. → 400), révélés dans le DTO booking **seulement ACCEPTED/COMPLETED** (jamais PENDING/REJECTED/CANCELLED). `BookingAcceptDTO` = `meetingLocation` seul (plus de lien à l'acceptation). `BookingResponseDTO` enrichi (`professorProfilePhoto/professorRating/professorReviewCount/subjectLabel/levelLabel`). `OfferDTO` `@JsonInclude(NON_NULL)` → lien/instructions absents de toute réponse publique (vérifié : offre 3 `/professors/1` = platform Zoom, 0 clé lien/instr). Notification étudiante créée à l'ajout/màj du lien. `BadRequestException` (`@ApplicationException`) mappée 400 dans `GlobalExceptionMapper` (remplace les `IllegalArgumentException` d'EJB → 500 en 400 : OfferService/BookingService/PhotoValidator/PhotoStorageService/ReviewService). `UploadResource` + `MeetingConfigDTO` enregistrés dans `ApplicationConfig.getClasses()` ; `/uploads` ajouté aux PATH publics de `AuthFilter`. Tests backend : **23/23 PASS** (9 PasswordUtil + 8 PhotoValidator + 6 UrlValidator).
- `ApplicationConfig.getClasses()` non vide ⇒ pas d'auto-scan : tout nouveau `@Provider`/filtre doit être ajouté à `getClasses()`.
- **Hibernate 6 garde les noms verbatim** → `hibernate.physical_naming_strategy=CamelCaseToUnderscoresNamingStrategy` dans persistence.xml (élimine les WARN "add column" au déploiement).
- **Échecs de login → 500 (bug #?)** : les exceptions lancées dans un EJB `@Stateless` (`AuthService.login`) sont enveloppées par l'EJB container en `jakarta.ejb.EJBException`, et `GlobalExceptionMapper` (ExceptionMapper<Throwable>) tombait sur le cas par défaut 500. Correctif : les 4 classes `com.coursconnect.exception.*` annotées `@ApplicationException(rollback = false)` → propagées sans wrapper. **Vérifié : login inconnu/mauvais mdp → 401.**
- `@Context ContainerRequestContext` en paramètre de méthode → `RESTEASY003880` (proxy Weld/RestEasy 6.2) → 500 sur tous les endpoints protégés. Correctif retenu : **helper `CurrentUserHolder`** (`@RequestScoped`, champ `User`) injecté (`@Inject`) et rempli par `AuthFilter` à l'auth ; ressources lisent `requireRole(currentUser, Role)` / `requireAuth(currentUser)` ; lecture du header token : `@HeaderParam("Authorization")` (plus aucun `@Context` dans les ressources — vérifié, reste uniquement dans les signatures de filtres `CorsFilter`/`AuthFilter` qui sont normales).
- **Bug de test détecté** : `PasswordUtilTest.generateTokenReturnsValidBase64` décodait les octets du token via `new String(byte[])` (charset UTF-8 ⇒ perte d'octets sur token aléatoire, flaky : 29 au lieu de 32). Corrigé : comparaison sur `byte[].length`. Vérifié 9/9 PASS, BUILD SUCCESS.
- **Frontend via proxy Vite → 404 HTML Undertow** : `api.ts` appelle `/api/...` mais le contexte WAR est `/coursconnect-api` ⇒ le proxy envoyait `/api/...` (aucun servlet) → Undertow `404 - Not Found`. Correctif `vite.config.ts` : `rewrite: (path) => path.replace(/^\/api/, '/coursconnect-api/api')` (+ vérifié : register/student réécrit → 201). **Redémarrer `npm run dev` pour recharger vite.config.ts.** Compte de test avec l'email de l'utilisateur supprimé en base.
- L'erreur `restriction is isn't exception` — non, évacuer : le `GlobalExceptionMapper` TOUJOURS mappe avant `Log` générant "Erreur interne du serveur"; désormais aucune stack non gérée pour les cas applicatifs.
- WildFly 33 : home image = `/opt/jboss/wildfly` (montage modules à cet endroit). Tag existant : `quay.io/wildfly/wildfly:33.0.1.Final-jdk21` (pas de `33.0.0.Final`).
- CLI jboss-cli / datasource : **env-driven depuis `wildfly/scripts/provision-datasource.sh`** (monté dans le conteneur via docker-compose, exécuté en `docker exec coursconnect-app sh /opt/jboss/wildfly/scripts/provision-datasource.sh`, idempotent : module + driver + datasource `CoursConnectDS` recréés). Lit `MYSQL_HOST` (défaut `coursconnect-db`), `MYSQL_PORT` (3306), `MYSQL_DATABASE` (coursconnect), `MYSQL_USER` (root), `MYSQL_PASSWORD` (coursconnect), `MYSQL_SSL_MODE` (DISABLED par défaut — local ; REQUIRED/VERIFY_CA pour Aiven, ajoute `useSSL=true&sslMode=...`). Déploiement WAR inchangé : `docker cp backend/target/coursconnect-api.war coursconnect-app:/opt/jboss/wildfly/` puis `deploy /opt/jboss/wildfly/coursconnect-api.war --force`. `backend/ds-fix.cli` (legacy, expression `${env.MYSQL_HOST:coursconnect-db}`) reste pour le fix UTF-8 ponctuel.
- **Préparation production MySQL/Aiven (livré côté code, aucune création Aiven)** : JNDI `java:jboss/datasources/CoursConnectDS` inchangé ; datasource provisionnée par `wildfly/scripts/provision-datasource.sh` à partir des env `MYSQL_HOST/MYSQL_PORT/MYSQL_DATABASE/MYSQL_USER/MYSQL_PASSWORD/MYSQL_SSL_MODE` (aucun hôte/mot de passe production en dur ; défauts = Docker local) ; TLS Aiven : `MYSQL_SSL_MODE=REQUIRED` (ou `VERIFY_CA`) → `useSSL=true&sslMode=...` dans l'URL JDBC, plus `serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8` conservés. `docker-compose.yml` : `MYSQL_HOST=coursconnect-db` (on continue le hostname local exact), `MYSQL_SSL_MODE=${MYSQL_SSL_MODE:-DISABLED}`, montage `./wildfly/scripts:/opt/jboss/wildfly/scripts:ro`. `.env.example` racine ajouté au bloc `MYSQL_*` (valeurs vides production, défauts documentés) ; frontend `VITE_API_URL` inchangé ; CORS `CORS_ALLOWED_ORIGINS` inchangé (env-driven, jamais `*` par défaut). `hbm2ddl.auto=update` conservé (dangereux en prod strict mais requis pour l'app existante) — recommandation : appliquer `schema-mvp.sql`+`seed-data.sql` puis passer à `validate` une fois la parité entités↔schéma confirmée.
- Compile Maven : `docker run --rm -v "C:/Users/MON PC/Documents/CoursConnect/backend:/app" -w /app maven:3.9-eclipse-temurin-21 mvn -q -DskipTests clean package`.
- Pb PowerShell E2E : ne PAS nommer de fonction `H` (`h` = alias `Get-History`, alias > fonction) ; dans l'API, `dayOfWeek` du créneau = chaîne `"MONDAY"` ; le DTO booking expose `professorResponse` (pas `rejectionReason`) ; `verifyProfessor` renvoie `ProfessorProfileDTO` (champ `firstName`, pas `name`) ; rating : `detail.averageRating` provient de la colonne `professors.average_rating`, recalculée (AVG reviews) à chaque nouveau/delete d'avis via `ProfessorRepository.updateRating` — le 4.80 affiché avant le nouvel avis était la valeur seed (stale vis-à-vis de MAJ) et se réconcilie après premier recompute (5.00), pas un bug.
- **Mojibake via `curl.exe`/PowerShell (leçon récente)** : décoder la sortie de `curl.exe` avec `ConvertFrom-Json` en PS 5.1 décode en cp1252 → ré-écrire un texte accentué (ex. PUT d'offre) **corrompt la DB** (`MathÃ©matiques`). Ne JAMAIS éditer de données textuelles via pipeline PS↔curl.exe ; préférer `node -e "fetch(...)"` (UTF-8 propre) ou `curl --data-binary @fichier` (fichier UTF-8). Déjà corrompu à nouveau puis réparé via Node (offre 3 : title/description accents + platform Zoom OK).

## Work State
### Completed
- **Toutes corrections backend** : AuthFilter (POST auth publics, préflight 204, comptes désactivés, **+ remplissage CurrentUserHolder**) ; GlobalExceptionMapper (WebApplicationException + 4 exceptions custom) ; **les 4 exceptions annotées `@ApplicationException(rollback=false)`** ; AuthResource (try/catch retirés + **logout `@HeaderParam`, me via holder**) ; Notification (`is_read`) ; schema-mvp.sql (colonnes legacy `professeurs` + procédure index conditionnelle) ; seed-data.sql (vrais hashes PBKDF2 ; matieres/niveaux insérés avant jointures) ; AvailabilityRepository.isSlotCovered ; BookingRepository.existsConflictForStudent (±12h) ; BookingService (propre offre, date requise/future, créneau couvert, conflits prof+étudiant) ; ReviewService (note 1..5, garde `lastName.charAt(0)`) ; AdminService.toggleUserStatus (blocage auto-désactivation) ; UpdateUserStatusDTO (@NotNull Boolean) ; BookingRejectDTO ; BookingResource.reject(DTO) ; ProfessorService (page≥0, size 1..100) ; frontend api.ts rejectBooking `{reason}`.
- **Refactoring holder complet** (plus aucun `@Context` dans les ressources) : CurrentUserHolder créé ; ProfessorResource, AdminResource, BookingResource, CityResource, LevelResource, SubjectResource, NotificationResource, ReviewResource, StudentResource, ProfesseurResource, AuthResource tous réécrits. Recompilé + redéployé (clean deploy, aucun WARN colonne).
- **Build : BUILD SUCCESS** (WAR 22,7 Mo) ; **Tests 9/9 PASS** après correctif du test générateur de token.
- **Étape 5 WildFly OK** : app sur **8081**, datasource CoursConnectDS testée `[true]`, WAR (contexte `/coursconnect-api`, path `/api`).
- **E2E complet PASS (vérifié en réel)** :
  - Guards : sans token 401 ; mauvais rôle 403 (student sur admin, prof sur student, admin sur prof/me) ; login inconnu 401 ; mauvais mdp 401 ; compte désactivé 401 ; réactivation → login OK ; admin self-disable 409.
  - Publiques : cities/subjects/levels actifs ; détail prof (rating+reviewCount), offers (3 : 80/150/70), availability (3), reviews.
  - Réservation : create (scheduledAt futur, offre, créneau couvert calculé depuis availability réelle) → notif prof (count=1) → accept ACCEPTED → complete COMPLETED → avis 5★ (id=5) → reviews publiques 2, note détail 5.00, stats admin bookings=10 reviews=5 ; cancel → CANCELLED ; reject (reason "Dispo incompatible") → REJECTED + `professorResponse` persisté.
  - Validations : date passée 400 ; son propre cours 403 ; sans offerId 400 ; doublon email 409.
  - Inscriptions : register étudiant → token+ROLE STUDENT, doublon 409 ; register prof (bio/exp/ville/matières/niveaux) → token, re-login OK, `/professors/me` id=11 verified=false subjects=1.
  - Admin : stats (users/profs/bookings/reviews) ; users 16→18 ; professors avec verified ; verify prof 5→true ; disable student→401 puis re-enable→OK ; profile étudiant PUT (phone, city).
- Infra : docker-compose (init SQL MVP+seed, port 8081, image 33.0.1.Final-jdk21, montage modules + deployments), vite.config.ts proxy 8081, module com.mysql fini + WAR dans wildfly/deployments, JacksonConfig ajouté au getClasses.
- **Parcours réservation corrigé (frontend)** : FicheProf (traduction jours/enum FR via `utils/labels.ts`, **résumé offre + aperçu date fin calculée + prix** dans le form, empty state "aucune offre", succès auto-dismiss 6s, reset offre après résa) ; dashboard étudiant (**annulation possible sur ACCEPTED** — backend le permettait déjà, onglets filtres Toutes/En attente/Confirmées/Terminées/Annulées/Refusées, cartes avec durée/lieu FR, **CTA "Prêt à commencer"** quand aucune résa + **carte "Prochain cours"** avec prochaine séance acceptée) ; dashboard professeur (**modal de refus avec motif personnalisé** → `rejectBooking(id, reason)`, onglets filtres, cartes enrichies date FR/durée/lieu, jours traduits dans créneaux/select, enums offre traduits, vue CANCELLED explicite). Backend : `BookingResponseDTO` + `BookingService.toDTO` exposent **`durationMinutes` + `locationType`** (pour les cartes). Vérifié : tsc + build (JS 276.8 kB / CSS 56.6 kB), WAR 22,7 Mo redéployé (`.deployed`), `/students/me/bookings` renvoie dur/loc, login+me+reject OK via proxy Vite.
- **Pb PowerShell E2E local** : `Invoke-WebRequest`/`Invoke-RestMethod` renvoient 404 à cause du proxy IE, alors que `curl.exe` et le curl in-container répondent 200 — utiliser `curl.exe` pour les vérifs (distinguer env vs code).
- **Déploiement WildFly (lié au bind-mount Windows)** : NE PLUS écrire `coursconnect-api.war` dans `wildfly/deployments/` (dossier scanné, **VIDE volontairement**) — copier le WAR dans le dossier scanné déclenche une **boucle de redéploiement toutes les ~10 s** (mtime unstable Docker Desktop) → épuise la Metaspace JVM → `OutOfMemoryError: Metaspace` → échec WeldStartService → tous les endpoints 404 + HTML brut dans l'UI. **Workflow sûr** : `backend/target/coursconnect-api.war` → `docker cp` vers `/opt/jboss/wildfly/coursconnect-api.war` (hors dossier scanné) → `jboss-cli deploy /opt/jboss/wildfly/coursconnect-api.war --force` (déploiement persistant ENABLED, aucun loop). Après `docker restart` le déploiement persiste (app se relance seule) ; après `docker compose up` (recréation), réappliquer conteneur → CLI (module+datasource+deploy).
- **Metaspace JVM ajustée dans le conteneur** (`/opt/jboss/wildfly/bin/standalone.conf`) : `-Xms64m -Xmx1024m -XX:MetaspaceSize=256M -XX:MaxMetaspaceSize=768m` (défaut image = 512m heap / 256m metaspace, trop juste pour le WAR 22 Mo). Attention : les guillemets `"` sont avalés par `docker exec coursconnect-app sed ...` direct — passer par un script (`%TEMP%\opencode\fix_metaspace.sh`) via `docker cp` + `sh`.
- **HTML ville/prof stell fix frontend** : `api.ts parseError` renvoie maintenant un message générique français si le corps d'erreur n'est pas du JSON (plus jamais de HTML Undertow collé dans l'UI).
- **Paiement & lieu (backend) livré et vérifié E2E réel** : enums `PaymentMethod`/`PaymentStatus` créés ; `Booking` + `BookingCreateDTO` + `BookingResponseDTO` + nouveaux `BookingAcceptDTO` + `BookingService` complétés — create() valide CASH-only (ONLINE → 409), calcule le montant (`amountOf`), gère le lieu par LocationType, `UNPAID` à la création ; `accept(BookingAcceptDTO)` (meetingLink ONLINE / adresse PROFESSOR_HOME) ; `markPaid()` COMPLETED-only → `PAID` + `paidAt` ; toDTO ré-expose les paiements + révèle lieu/lien seulement ACCEPTED/COMPLETED + fallbacks legacy ; `Professor`/`ProfessorProfileDTO`/`UpdateProfessorDTO`/`ProfessorService` + champ `teachingAddress` ; `BookingResource` : accept(DTO null-safe), `PUT /bookings/{id}/payment/mark-paid` ; `schema-mvp.sql` (SET NAMES utf8mb4 + colonnes). **Vérifié via curl (8081 + proxy Vite)** : ONLINE→409 message honnête ; CASH→UNPAID montant 60/70/80 DH corrects ; PROFESSOR_HOME sans adresse OK + accept révèle l'adresse ; STUDENT_HOME sans adresse→409 « Veuillez indiquer l'adresse du rendez-vous », avec adresse→201 ; mark-paid avant COMPLETED→409, après→PAID+paidAt, double→409 « déjà encaissé » ; accents propres en base (`meeting_location` « Résidence Al Amal… ») ; aucune fuite de lieu/paiement sur `/professors/1` (keys sans meeting/payment). hbm2ddl=update a ajouté les colonnes automatiquement (énums CASH/ONLINE et UNPAID…/REFUNDED, amount DECIMAL(8,2), meeting_*, teaching_address).
- **Paiement & lieu (frontend) livré + compilé** : types (PaymentMethod/PaymentStatus/BookingCreateData, Booking + amount/currency/payment* /meeting*, ProfessorProfile.teachingAddress) ; api.ts (`createBooking(data: BookingCreateData)`, `acceptBooking(id, dto?)` avec body, `markBookingPaid(id)`) ; labels (`PAYMENT_METHOD_LABELS`, `PAYMENT_STATUS_LABELS`, `formatDateTimeRange` corrige aussi un bug latent formatTime sur datetime complet « 2026-0 – NaN:00 ») ; **FicheProf = réservation en 4 étapes** (Date & heure → Lieu → Paiement [Espèces sélectionnée, En ligne « Bientôt disponible » désactivé] → Récapitulatif [montant total DH, Retour/Confirmer et envoyer]) + **écran de confirmation** « Demande envoyée ! » avec [Voir ma réservation → /student?tab=bookings] / [Rechercher d'autres cours] / nouvelle demande ; dashboard étudiant (badge paiement « En espèces · Non réglé/Payé » + badge statut, montant DH, « Payé le … », bloc `.booking-meeting` révélant adresse/lien après confirmation, tab initial lu depuis ?tab=) ; dashboard professeur (badges + montant à encaisser, bouton **« Encaisser le paiement »** sur COMPLETED cash non payé, **modal d'acceptation pour cours en ligne** avec lien de séance optionnel, adresse d'enseignement dans profil, typo « contacter-moi »→« contactez-moi »). Vérifié : `tsc --noEmit` propre + `npm run build` OK (JS 287.9 kB / CSS 61.1 kB + 11.2 gzip).
- **Cause racine encodage appliquée** : réparation DB complète (cities/levels/subjects/bios/offers/matieres/niveaux/notifications/reviews) via `CONVERT ... USING utf8mb4` + `backend/repair-utf8.sql` ; datasource JDBC `characterEncoding=UTF-8` via `backend/ds-fix.cli` + `:reload` (endpoints 200) ; vérifié propre en base et via API.
- Responsive : `--max-width` 1240→1400px ; grille `dash` 256px+1fr ; filtres recherche → drawer slide-in ≤1024px (overlay+Esc+body scroll lock) ; search bar stacks ≤900px ; hamburger header ≤900px ; prof-grid 3→2→1 col ; steps 4→3→2→1 col ; form-row column ≤768px ; footer 1 col ≤768px ; stats 4→2→1 col ; modal bottom-sheet ≤480px ; pagination wrap ; dropdown user-menu max-width safety ; fiche-prof 1100px ; page data table via `.table-wrap` overflow-x.
- **Overflow fiche prof ≤1024px (régression date-strip) — cause racine trouvée et corrigée** : `.fiche-prof.container` (`margin: 0 auto` + `width:auto`) est un **flex item de `.main` (flex-column)** → les marges auto court-circuitent le stretching ; sa largeur devient `fit-content` (min-content du contenu) → la nouvelle `date-strip` (14 boutons `day-choice flex-shrink:0`, `nowrap`, min-content 60+px) élargissait la page (scrollW 1091 au lieu de 1024, même avec `min-width:0` qui ne force pas un `width:auto`). **Fix : `width:100%` sur `.fiche-prof`** (couplé au `max-width:1100px` existant, `min-width:0` retiré) → la `date-strip` scroll ∫ elle-même (`overflow-x:auto`) aux largeurs étroites. Vérifié : `probe7.js` (retrait `.slot-picker` → 1024), `probe8.js` (seule `width:100%` et `overflow-x:clip` sur `.main` corrigent, pas `min-width`/`overflow:hidden` du `.slot-picker`), **overflow 0px @ 1440/1280/1024/768/430/390/360** (les `day-choice` à `right>viewport` sont DANS le scroll container — `scrollW==viewport`, pas une fuite).
- **Recherche : « Erreur inconnue » + responsive verrouillés (vérifié navigateur réel, 7 viewports par script playwright-core/Edge headless)** : cause racine du « Erreur inconnue » = **backend WildFly/MySQL indisponible au moment du test** (Docker Desktop éteint) → requête `/api/professors` échouait et le fallback `parseError` « Erreur inconnue » (corps vide) s'affichait ; **aucune faute backend structurelle** (GET /professors public, CORS 5173 OK, JSON `PagedResult{contenu,page,taille,totalElements,totalPages}` conforme, filtres courseType/sort/clamp OK). Correctifs `frontend/src/services/api.ts` : `rawFetch` intercepte les erreurs réseau/TypeError → « Impossible de contacter le serveur. Vérifiez que le backend est démarré. » ; `buildErrorMessage` : JSON backend sûr (error/details/message, cap 300), HTML ou texte proxy (`error occurred while trying to proxy`, 502/503/504) → messages FR génériques, autre texte non-JSON → « Le serveur a retourné une erreur (STATUS). » ; 200 non-JSON → « réponse invalide » ; authFetch/publicFetch passent par rawFetch. CSS `index.css` : `.error-banner` flex-wrap ; ≤640px `.results__toolbar` en colonne + `.results__sort` pleine largeur ; ≤480px header desktop déconnecté (`.header__actions .btn--ghost/.btn--primary`) masqués (le drawer mobile contient déjà Connexion/S'inscrire) — corrige un vrai overflow constaté (scrollW 508px à 360px de viewport). Vérifié : **overflow 0px @ 1440/1280/1024/768/430/390/360** (Recherche + Home + header connecté), drawer 340px/86vw OK, skeleton de chargement, erreurs réseau/500/JSON safe, Réessayer OK, pagination OK, empty state « Aucun professeur trouvé » ; backend `mvn clean package` 9/9 tests PASS ; `/api/{professors,cities,subjects,levels}` 200 via 8081 + proxy Vite 5173 (vite.config rewrite intact). Stack remontée (Docker Desktop + `coursconnect-app` + `coursconnect-db`, déploiement persistant préservé) ; dev server Vite relancé.
- **Recherche marketplace v2 (backend + frontend, livré et vérifié)** : ajout d'un **recherche texte libre** au backend — paramètre `search` sur `GET /professors` (nom/prénom complet, nom de matière, nom de ville ; collation MySQL ci → insensible casse/accents), implémenté dans `SearchCriteriaDTO` + `ProfessorRepository.appendFilters/setParams` (LOWER + CONCAT + subquery subjects, même clause dans countSearch) + `ProfessorService.search` (trim) + `ProfessorResource` (`@QueryParam search`) ; WAR 22 742 184 o redéployé via CLI (`.deployed` OK, `mvn test` 9/9 PASS), vérifié curl : `search=Moh`→5, `math`→5, `rabat`→1, inconnu→0. Frontend : **`Recherche.tsx`** — barre de recherche (`results__search`, placeholder « Rechercher un professeur, une matière ou une ville... », icône + clear + submit Enter, chip « Recherche : « … » »), **filtres avancés repliables** (toggle aria-expanded, auto-ouvre si Type de cours/Tarif/Note min active), labels tri → rating « Mieux notés » (tri exposés = backend uniquement), empty state hiérarchisé + bouton « Effacer les filtres » réel (chips>0), header « X professeurs trouvés » (totalElements réel) ; **`ProfesseurCard.tsx` refondu** : `article` (plus de Link-serveur imbriqué), cover gradient + Avatar lg + bouton favori ♥ (voir bullet Favoris ci-dessous — **initialement localStorage, désormais API backend authentifiée**), badge « Vérifié » (success), note + nb avis, matière principale (ellipsis), meta niveaux · ville · lieux (offers→`formatLocationType` dédupliqués), bio clamp 2, pied « À partir de X DH / heure » + CTA « Voir le profil » (`btn--primary` Link) ; **`LoadingState` variant `prof`** (skeleton aligné nouvelle carte ; admin conserve le défaut) ; `Accueil.tsx` passe aussi en variant prof ; icône `heart` ajoutée à Icon.tsx ; types `SearchCriteria.search`. Vérifié headless (check4.js + check.js) : chip + param `search=Math` réel (5 résultats), clear → 0 chips, favori persiste après reload, toggle avancé aria-expanded, empty state reset → 12 résultats + URL nettoyée, Home OK (6 cartes/covers/favs, 0 overflow, 0 JS erreur) ; **overflow 0px @ 1440/1280/1024/768/430/390/360**, erreurs réseau/500/JSON/skeleton inchangées. `tsc --noEmit` propre + `npm run build` OK (JS 292.30 kB / CSS 64.62 kB). NB : le modèle ne lit pas les images → vérification visuelle pixel par l'utilisateur recommandée (captures `%TEMP%\opencode\shots\vw-*.png`, `recherche-v2.png`).

- **Availability picker + réservation 4 étapes (FicheProf) livré et vérifié headless** : `labels.ts` + `BOOKING_STATUS_LABELS`/`formatBookingStatus` ; **FicheProf.tsx** — helpers `toMinutes`/`dateKey`/`buildDateOptions` (14 j)//`buildTimeSlots` (slots 60 min 8:00–20:00, durées passées du jour disabled) + `isSlotCoveredByAvailabilities` corrigé (compare des minutes, l'ancien comparait des chaînes `"14:00:00"<="14:00"`), section **Disponibilités `#dispo`** = date-strip (`day-choice` 14 j, disabled si aucun créneau ce jour) + time-grid (`slot-chip`, disabled si couvert/selection), sélection → `scheduledAt` + scroll résa ; booking **steps 1 Offre (cartes radio, warn « Cette durée dépasse le créneau disponible. » si durée offre > créneau) → 2 Lieu (adresse si STUDENT_HOME/OTHER, warning = source de vérité backend) → 3 Paiement (CASH pré-sélectionné, ONLINE désactivé) → 4 Récapitulatif** (rows Professeur/Cours/Date/Horaires/Durée/Type/Prix horaire/Lieu/Paiement/Montant total) + « Confirmer la réservation » ; écran succès **« Réservation confirmée ! »** (recap + Statut `formatBookingStatus` + « Voir mes réservations »→/student?tab=bookings + « Rechercher d'autres cours »). CSS : `date-strip`/`day-choice`/`time-grid`/`slot-chip.is-disabled`/`booking-offers`/`booking-offer`/`booking-recap`. **Vérifié `check-booking.js` (headless Edge)** : **overflow 0px @ 1440/1280/1024/768/430/390/360** (jour à `right>viewport` = DANS le scroll container, scrollW==viewport), flux étudiant complet (hint « Créneau choisi : lundi 14 septembre 2026 à 14:00 », 3 cartes offre, cash pré-coché, recap 10 rows + confirmBtn, 0 erreurs JS), guards (Continuer sans créneau → « Veuillez choisir une date et une heure disponibles dans la section Disponibilités. » ; offre 120 min à 17:00 → warn `1`, à 14:00 → `0` correct). `npm run build` OK (JS 299.93 kB / CSS 68.91 kB) ; `mvn test` backend **9/9 PASS**.
- **Amélioration expérience confiance + avis (frontend only, livré et vérifié)** : seulement les fichiers frontend touchés + aucun changement backend (réutilisation stricte de l'API Review existante, anti-doublon déjà garanti : 1 avis par réservation via `@OneToOne unique booking_id` + backend 409 « Vous avez déjà laissé un avis pour ce cours »). **`ProfesseurCard.tsx`** : badge « Vérifié » → **« Profil vérifié »** (gating `professor.verified` inchangé, « Ne pas afficher 'Profil vérifié' si le backend ne confirme pas la vérification ») ; format note/avis « 5.0 · 2 avis » (pas de parenthèses, « Aucun avis » si aucune note). **`FicheProf.tsx`** — section `#avis` : **section confiance avec `RatingSummary`** (note moyenne + distribution) ; **liste des avis** (auteur + date longue FR `formatDateFR` + étoiles `RatingStarsDisplay` + commentaire ou « Avis sans commentaire. ») ; **suppression du faux badge « Élève vérifié »** codé en dur sur chaque avis ; état vide : **« Aucun avis pour le moment. »** + texte secondaire « Les avis apparaîtront ici après les cours terminés. Soyez la première personne à partager votre expérience. » ; hero badge **« Profil vérifié »** (cohérent carte). **`RatingStars.tsx`** — `value=0` géré proprement (label « Sélectionnez une note », aria « Aucune note sélectionnée ») ; `tabIndex` star 1 quand rien sélectionné. **`RatingSummary.tsx`** — ajout libellé « Note moyenne » au-dessus de la moyenne. **`ReviewModal.tsx`** — formulaire professionnel : note initiale **0** (doit être choisie, submit disabled tant que `rating < 1`) ; `maxLength={2000}` + compteur « x / 2000 caractères » ; labels « Votre note * » (obligatoire) + « Commentaire (optionnel) » + avertissement « Votre avis sera affiché publiquement... » ; annulation ferme la modale. **`student/Dashboard.tsx`** — `setReviewError('')` à l'ouverture du modal (nettoyage erreur précédente) + notice « Avis publié. Merci ! » auto-dismiss 6s via `useEffect`. **`professor/Dashboard.tsx`** — onglet « Avis reçus » fix : étoiles `size={1}` → `size={15}` (étaient invisibles) + date longue via `formatDateFR` + placeholder « Avis sans commentaire. » si `comment` absent. **CSS `index.css`** : `.review-empty`/`.review-empty__title`/`.review-empty__text` (dashed border + surface-2 background), `.review-summary__label` (uppercase 0.78rem), `.review-item__who`/`.review-item__stars` (`margin-left:auto`) / `.review-item__no-comment` (muted italic), `.review-step__required` (red), `.review-step__optional` (muted), `.review-step__counter` (right-aligned, `.is-error` red), `.booking-meeting a` (`overflow-wrap:anywhere` + `word-break:break-word` — corrige overflow 2px à 360px causé par URL de séance), `.review-item__head` (`flex-wrap:wrap`). **Vérifié `check-trust.js` (headless Edge)** : **overflow 0px @ 1440/1280/1024/768/430/390/360** (FicheProf + Home + Recherche + Student dashboard) ; prof 1 : hero badge « Profil vérifié » visible + résumé « Note moyenne 5.0 /5 · N avis » + liste (auteur/date FR/étoiles/commentaire visible, aucun « Élève vérifié ») ; prof 11 (non vérifié, sans avis) : état vide « Aucun avis pour le moment. » + aucun résumé + aucun badge vérifié ; prof 9 (non vérifié, 0 reviews rows) : même comportement état vide ; cartes Recherche : ahmed « Profil vérifié » + « 5.0 ... N avis », khalid aucun badge ; **formulaire avis live** (Omar, réservation COMPLETED sans avis) : submit disabled @0★, label « Sélectionnez une note » → 5★ → label « Excellent » + submit enabled → 2000 caractères caps + counter « 2000/2000 » submit toujours enabled → commentaire 65 caractères → submit → « Avis publié. Merci ! » + badge « Avis publié pour ce cours » + refresh (booking rafraîchi, button disparu) + 0 erreurs JS ; **anti-doublon backend** : POST deux fois sur la même réservation → 201 puis 409 « Vous avez déjà laissé un avis pour ce cours » (testé live, reviews nettoyées après vérification) ; modal bottom-sheet ≤480 vérifié interactif à 360px (star 5 → enabled, counteur live). `tsc --noEmit` propre + `npm run build` OK (JS 301.03 kB / CSS 69.85 kB) ; `mvn test` backend **9/9 PASS** (aucun fichier backend modifié).

- **Favoris professeurs (backend + frontend, livré et vérifié)** : **aucune API favoris n'existait côté backend** (grep initial vide) — le frontend utilisait un localStorage (`cc_favorite_professors`) → **remplacé par un backend authentifié** à la demande utilisateur. **Backend** : entité `ProfessorFavorite` (clé composite `@IdClass ProfessorFavoriteId` student_id+professor_id, table `professor_favorites` ajoutée à `schema-mvp.sql` + auto-créée par hbm2ddl=update au déploiement) ; `ProfessorFavoriteRepository` (find/exists/save/delete + `findProfessorIdsByStudent` + `findProfessorsByStudent` triés createdAt DESC) ; `FavoriteService` (add idempotent→boolean, remove idempotent, getIds, getFavorites→`professorService.toCardDTO` réutilisé) ; endpoints dans `StudentResource` (déjà enregistré dans ApplicationConfig) : `GET /students/me/favorites` (liste `ProfessorCardDTO`), `GET /students/me/favorites/ids`, `POST /students/me/favorites/{professorId}` (**201** si créé, **204** si déjà présent), `DELETE /students/me/favorites/{professorId}` (**204** idempotent) — tous rôle STUDENT. **Frontend** : `api.ts` (+`getMyFavoriteProfessors/getMyFavoriteIds/addFavorite/removeFavorite` ; **`handleResponse` corrigé** pour accepter les corps vides sur 2xx — les 201 sans body étaient traités comme erreurs « réponse invalide ») ; **`utils/favorites.ts` réécrit en store authentifié** (lit `cc_token`/`cc_user`, garde l'interface `{isFav: (id)=>boolean, toggle:(id)=>Promise<'added'|'removed'|'needs-auth'|'error'>}`, `loadFavoritesIfNeeded` sur montage du hook + émission réseau, flux à chargement anti-doublon) ; **`ProfesseurCard.tsx`** cœur auth-aware (non connecté → `/login` ; non STUDENT → erreur « Les favoris sont réservés aux élèves. » ; busy désactive le bouton + `is-busy` ; erreur inline `role="alert"` sous le cœur ; aria-pressed/aria-label conservés) ; **`FicheProf.tsx`** bouton favori identique (loading + erreur hero) ; **nouvelle page `pages/Favoris.tsx`** (route `/favoris` `ProtectedRoute roles=['STUDENT']`, titre « Mes favoris (n) », grille `prof-grid` réutilisée → cartes `ProfesseurCard` retirées dès que dés-favorisées, état vide exact **« Aucun professeur enregistré »** / **« Ajoutez vos professeurs préférés pour les retrouver rapidement. »** / CTA **« Rechercher un professeur »** → `/recherche`, loading `variant="prof"`, error-banner + Réessayer) ; « Mes favoris » ajouté au menu utilisateur Header (desktop) et au drawer mobile. **Vérifié** : E2E curl (VIDE→201→204 idempotent→ids [1]→liste carte ; DELETE 204 ×2 idempotent ; 99999→404 « Professeur non trouvé » ; sans token→401 ; ADMIN→403 « Accès réservé aux étudiants ») ; headless `check-fav.js` **38/38 PASS** (ajout→cœur actif→persistance après reload, dés-favori→carte retirée→état vide exact, CTA exact, redirections `/login` non connecté carte+fiche, menu utilisateur + drawer « Mes favoris », **overflow 0px @ 1440/1280/1024/768/430/390/360** sur `/favoris` vide, 0 erreurs JS) ; `tsc --noEmit` propre ; `npm run build` OK (JS 304.60 kB / CSS 70.73 kB) ; `mvn test` backend **9/9 PASS**. Base laissée propre (favoris d'omar vides). NB debug headless : `ctx.addInitScript(fn, token)` doit passer la **fonction + l'arg** (le `cc_token` doit être réel pour éviter la redirection `/login` de `authFetch` sur 401).

- **Cours en ligne « lien par réservation » (backend, livré et vérifié E2E réel)** : `UrlValidator.isHttpsUrl` (https strict → 400 sur http://, javascript:, data:, file:, ftp:, « not a url », https:// vide, null) ; `OfferDTO` débarrassé de `meetingLink`/`meetingInstructions` (l'offre ne garde que `meetingPlatform`, publique) ; `OfferService.applyDto/toDTO` n'écrivent plus ces champs ; `MeetingConfigDTO` (link ≤500 / platform ≤50 / instructions ≤2000) ; `BookingAcceptDTO` réduit à `meetingLocation` ; `BookingService` : `accept()` sans gestion de lien, `saveMeetingConfig` (ONLINE uniquement + ACCEPTED/COMPLETED uniquement + https strict + notification étudiant), `deleteMeetingConfig` (link+instructions → null, platform conservée), `getParticipantBooking` (ownership étudiant OU professeur → sinon 403/404) ; `BookingResponseDTO.toDTO` enrichi `professorRating/professorReviewCount/professorProfilePhoto/subjectLabel/levelLabel` (helper `firstName(List,fn)`) ; `BookingResource` : `GET /bookings/{id}` (requireAuth), `PUT /bookings/{id}/meeting` (PROFESSOR, `@Valid`), `DELETE /bookings/{id}/meeting` (PROFESSOR). **E2E curl vérié** : PENDING sans lien → PUT meeting étudiant 403 → 409 si PENDING → accept `{}` → http:// 400 → javascript: 400 → https OK → étudiant voit → DELETE OK → notification créée ; `mvn test` **23/23 PASS** ; WAR redéployé `--force` (CLI).
- **Dashboard étudiant routé + zone lien de séance prof (frontend, livré et vérifié headless 102/102)** : refonte en 5 pages (plus d'onglets dans une page). Nouveaux fichiers : `components/dashboard/StudentShell.tsx` (sidebar : Tableau de bord, Rechercher un professeur, Mes Réservations, Favoris, Notifications, Mon Profil, Déconnexion, badge count en attente), `pages/student/Home.tsx` (hero « Bonjour … », stats, **Prochains cours ≤3**, Dernières réservations, CTA), `pages/student/StudentBookings.tsx` (**onglets Toutes / En attente / Confirmées [ACCEPTED toutes dates] / À venir [PENDING+ACCEPTED futurs] / Terminées / Annulées [CANCELLED + REJECTED]**, filtre dans l'URL `?filter=`, cartes enrichies photo·note·avis·sujet·niveau·montant·plateforme·statut, bloc séance Prêt/Lien à venir avec Rejoindre, annulation, avis, badge « Avis publié »), `pages/student/BookingDetail.tsx` (sections Professeur/Cours réservé/Paiement/Statut/Cours en ligne/Lieu + messages contextuels par statut), `pages/student/Profile.tsx`, `pages/student/Notifications.tsx` (liste + « Tout marquer comme lu »). Routes `/student*` dans `App.tsx` ; ancien `pages/student/Dashboard.tsx` **supprimé** ; lien succès `FicheProf` → `/student/reservations`. **Dashboard prof** : offre « en ligne » = plateforme préférée + aide « le lien est ajouté à la réservation après acceptation » ; liste offres avec compat ; modal d'acceptation simple (rappel `ONLINE_ACCEPT_HINT`) ; **zone « Cours en ligne » par réservation** (badge « Lien ajouté »/« Lien à ajouter », éditeur inline platform/lien/instructions + Enregistrer/Supprimer/Annuler, erreurs 400 affichées, rechargement) ; Vue d'ensemble : compteur « X confirmé(s) · Y lien(s) ajouté(s) · Z à ajouter » + CTA « Gérer les liens ». API : `getBooking/saveMeetingConfig/deleteMeetingConfig`, `acceptBooking({meetingLocation?})` ; `labels.ts` : `isSafeMeetingUrl` https-only, `MEETING_LINK_HELP`, `ONLINE_ACCEPT_HINT`. `tsc --noEmit` propre + `npm run build` OK (JS 323.24 kB / CSS 75.12 kB).
- **Vérifs headless dashboard (check-dash.js)** : 102 checks — accueil (hero/stats/prochains cours/dernières résa), onglets (`Toutes (11)`, Annulées = CANCELLED+REJECTED, filtre URL), détail 27 (6 sections + « Lien à venir »), profil/notifs (liste + non-lues + « Tout marquer comme lu »), prof (compteur cours en ligne, éditeur lien : préfill platform « Zoom », Enregistrer → badge « Lien ajouté » + href `zoom.us/j/987654321`, étudiant voit « Prêt »+« Rejoindre »+instructions, restore DELETE), **overflow 0 px @ 1440/1280/1024/768/430/390/360** sur les 5 pages étudiantes (+ prof) sans erreur JS. Fix inclus : `matchesFilter` CANCELLED gère aussi REJECTED ; `measureOverflow` via `querySelectorAll('body *')` ; `.dash-card__title` `flex-wrap: wrap` (bouton « Tout marquer comme lu » débordait à 360 px). **Donnée restaurée** : offre 3 (prof Ahmed) `meetingPlatform=Zoom` + accents title/description réparés via Node `fetch` (après corruption cp1252 de mon PUT PS, voir leçon mojibake) ; booking 10 = COMPLETED ONLINE **sans lien** (restauré par le test).
- **Dashboard professeur routé + Notifications fiables (backend + frontend, livré et vérifié E2E 27/27 + headless 157/157)** : **backend** — `Notification.referenceId` (colonne `reference_id`) écrit sur **toutes** les notifications de réservation (création/acceptation/lien/annulation/refus/terminaison/annulation prof) ; `PUT /notifications/{id}/read` (404 si notif d'autrui) ; `BookingService.cancel` message enrichi « {Prénom} {Nom} a annulé sa réservation de {Offre} prévue le {date FR} à {heure}. » (type `BOOKING_CANCELLED`) ; **`cancelByProfessor`** (`PUT /bookings/{id}/professor-cancel`, PROPOSSEUR, → notif étudiant) ; `checkCancellable()` → **409 anti-doublon sans nouvelle notif** ; `BookingResponseDTO.studentProfilePhoto`. **Frontend** — `components/dashboard/ProfessorShell.tsx` (sidebar 6 entrées + Déconnexion, **badge unread** via `hooks/useUnreadCount.ts`) ; ancien `Dashboard.tsx` (monolithe) **supprimé** au profit de 9 routes : `/professor` `Home` (Bonjour {prénom}, **4 KPI réels**, **« À faire »** dynamique sinon « ✓ Tout est à jour », Prochains cours ≤3 avec avatar étudiant + Rejoindre/Ajouter le lien, Dernières réservations, Aperçu des offres), `ProfessorBookings` (onglets Toutes/En attente/Confirmées/À venir/Terminées/Annulées + **Propositions**, `?filter=`, badge compte propositions), `ProfessorBookingDetail` (sections Élève/Cours réservé/Paiement/Statut/Cours en ligne éditeur/Lieu + actions accepter/refuser(motif)/terminer/encaisser/annuler-prof), `ProfessorNotifications` (**clic → marque lu + `/professor/reservations/{referenceId}`**, « Tout marquer comme lu »), `ProfessorProfile` (photo/upload/suppression + formulaire + **Avis reçus** (étoiles *15*) + **Paramètres** email/rôle/vérifié/fiche publique), `ProfessorOffers` + `ProfessorOfferForm` (+`/new`, `/:id/edit`), `ProfessorAvailability`. Reuse : `components/dashboard/BookingCard.tsx`. CSS ajouté : `.todo-item*`, `.todo-empty`, `.notif-item__link`/`__cta`, `flex-wrap:wrap` sur `.booking-item__prof` (badge paiement débordait à 360 px). Vérifié : `mvn test` **23/23** ; E2E `e2e-prof-notify.mjs` **27/27** ; headless `check-prof.js` **157/157** (7 viewports overflow 0 sur Home/Réservations/Notifs/Profil/Offres/Dispo/Détail + flux interactifs, éditeur lien préfill Zoom si link absent, 0 JS error) ; `tsc --noEmit` propre + `npm run build` OK (JS 348.72 kB / CSS 76.16 kB). Données : bookings 29/30 de test + notifications nettoyées ; booking 10 restauré sans lien.

### Active
- **Vérification visuelle utilisateur en attente** : tester dans le navigateur aux breakpoints 1440→375px : **dashboard prof routé** (9 pages /professor* : Home 4 KPI + « À faire », sidebar badge unread, page Notifications cliquable → détail réservation, page Profil photo+avis+paramètres, offres+form, dispo ; zone « Cours en ligne » éditeur lien par réservation), **dashboard étudiant routé** (/student, réservations, réservations/:id, profil, notifications), /professeur/:id (date-strip, stepper 4 étapes, confiance/avis, badge plateforme, écran succès, photo), /recherche, **/favoris**, /login — s'assurer que la typographie, l'espacement et les textes sont corrects aux 7 viewports (captures headless dispo : `%TEMP%\opencode\shots\prof-*.png`, `st-*.png`, `tr-*.png`, `bk-*.png`, `fav-*.png`, `on-*.png`).

### Blocked
- **(--) Déploiement Vercel réel (bloqué par info utilisateur)** : pas de CLI/token Vercel sur la machine (`vercel` absent, `VERCEL_TOKEN` non défini) et **URL HTTPS du backend production inconnue** → ne pas inventer. Toute la préparation (vercel.json, VITE_API_URL, .env.example, build OK) est faite ; reste à fournir : token Vercel ou `vercel login`, et l'URL réelle du backend Jakarta EE de production (+ CORS `CORS_ALLOWED_ORIGINS` à mettre à jour).

## Next Move
1. **Vérification visuelle utilisateur** : tester dans le navigateur (breakpoints 1440→375) le **dashboard prof routé** (Home 4 KPI + liste « À faire », sidebar 6 entrées + badge unread, Notifications cliquables → `/professor/reservations/{referenceId}`, Profil avec photo/upload + Avis reçus + Paramètres, Offres + formulaire, Dispo), le **dashboard élève** (/student, réservations, réservations/:id, profil, notifications), /professeur/:id (date-strip, stepper 4 étapes, confiance/avis, badge plateforme, récapitulatif, écran succès, photo), /recherche, /favoris, /login — confirmer typographie/espacements/textes (captures headless dispo : `%TEMP%\opencode\shots\prof-*.png`, `st-*.png`, `tr-*.png`, `fav-*.png`, `on-*.png`).
2. **Déploiement Vercel réel** (bloqué) : une fois fournis token Vercel (ou `vercel login`) + URL HTTPS du backend production → `vercel link` dans `frontend/`, définir `VITE_API_URL`, `vercel deploy --prod`. Puis tester depuis le vrai domaine (login admin/student/professor, routes directes SPA, aucun appel localhost dans le Network) et mettre à jour `CORS_ALLOWED_ORIGINS` du backend avec le domaine Vercel.
3. **Rapport final 11 items** (reporté — le RAPPORT-FINAL.md courant documente la tâche « dashboard professeur routé + notifications fiables », livré + documenté ; le rapport global des 11 bugs antérieurs reste à consolider si demandé) : bugs corrigés (injection @Context → RESTEASY003880 ; login 500 → @ApplicationException ; Jackson LocalDateTime 400 → JavaTimeModule ; Hibernate naming → snake_case ; seed FK matieres/niveaux ; WildFly tag/montage ; test flaky token ; DTO booking sans durée/lieu → ajout ; mojibake DB + JDBC latin1 → UTF-8 ; formatTime sur datetime → formatDateTimeRange ; isSlotCovered chaînes → minutes + overflow date-strip → `width:100%`), comportements vérifiés E2E, fichiers/entités modifiés, points restants (paiement en ligne, données réelles, anciennes notifications sans referenceId).

## Relevant Files
- `backend/src/main/java/com/coursconnect/config/CurrentUserHolder.java` : **créé** (bean `@RequestScoped`, champ `User`).
- `backend/src/main/java/com/coursconnect/config/JacksonConfig.java` : **créé** (ObjectMapper + JavaTimeModule), enregistré dans ApplicationConfig.
- `backend/src/main/java/com/coursconnect/config/AuthFilter.java` : pré-matching ; injecte CurrentUserHolder ; `/professeurs/me*` désormais protégé (public uniquement search GET).
- `backend/src/main/java/com/coursconnect/exception/{Unauthorized,Forbidden,Conflict,NotFound}Exception.java` : **annotées `@ApplicationException(rollback=false)`** (EJB unwrap).
- `backend/src/main/java/com/coursconnect/rest/*.java` : **tous réécrits sans `@Context ContainerRequestContext`** (holder + `@HeaderParam` pour AuthResource).
- `backend/src/test/java/com/coursconnect/config/PasswordUtilTest.java` : **test token corrigé** (byte[]).
- `backend/src/main/resources/META-INF/persistence.xml` : stratégie naming snake_case.
- `sql/schema-mvp.sql`, `sql/seed-data.sql` : corrigés + appliqués.
- `docker-compose.yml`, `frontend/vite.config.ts` : port 8081, init SQL, montages, image jdk21, **proxy `/api` → `/coursconnect-api/api` (rewrite)**.
- `wildfly/modules/com/mysql/main/` (module.xml + jar MySQL 8.3.0) ; **`wildfly/deployments/` volontairement VIDE** (scanner = boucle de redéploiement, voir plus haut) ; déploiement géré par CLI depuis `/opt/jboss/wildfly/coursconnect-api.war`.
- `backend/target/coursconnect-api.war` : régénéré après refactoring + exceptions (WAR 22,7 Mo).
- `backend/src/main/java/com/coursconnect/dto/BookingResponseDTO.java` + `.../service/BookingService.java` : **`durationMinutes` + `locationType` ajoutés** au DTO booking.
- `frontend/src/utils/labels.ts` : **créé** — traductions FR (`formatDay`, `formatCourseType`, `formatLocationType`, `formatDateFR`, `formatTimeFR`, `computeEndTime`, `DAY_ORDER`, `PAYMENT_METHOD_LABELS`, `PAYMENT_STATUS_LABELS`, `formatDateTimeRange`, `BOOKING_STATUS_LABELS` + `formatBookingStatus`, **`isSafeMeetingUrl`/`safeMeetingUrl`, `MEETING_PLATFORMS`, `isValidMeetingPlatform`, `MEETING_LINK_HELP`**) réutilisées dans FicheProf + dashboards.
- `frontend/src/pages/FicheProf.tsx` : parcours réservation corrigé (modal refus motif, onglets filtres, CTA "Prêt à commencer", carte "Prochain cours", annulation ACCEPTED, **stepper 4 étapes + écran confirmation**, **badges paiement + montant DH + bloc lieu/lien après confirmation**, **encaisser le paiement**, **modal d'acceptation en ligne avec lien**, adresse d'enseignement profil, typo fix) + **availability picker `#dispo`** (date-strip 14 j + time-grid 60 min, helpers `toMinutes`/`dateKey`/`buildDateOptions`/`buildTimeSlots`, `isSlotCoveredByAvailabilities` comparant les minutes, reset `selectedDate`/`selectedTime`, écran succès « Réservation confirmée ! »).
- `frontend/src/types/index.ts` : `Booking` étendu (`durationMinutes?`, `locationType?` + `meetingPlatform?`/`meetingInstructions?`) + `PaymentMethod`/`PaymentStatus`/`BookingCreateData` + `ProfessorProfile.teachingAddress`/`UpdateProfessorData` + `SearchCriteria.search` + `Offer.meetingPlatform/meetingLink/meetingInstructions`.
- `backend/src/main/java/com/coursconnect/dto/SearchCriteriaDTO.java` + `.../repository/ProfessorRepository.java` + `.../service/ProfessorService.java` + `.../rest/ProfessorResource.java` (**recherche texte libre**) : paramètre `search` (LOWER + CONCAT nom complet + ville + subquery subjects) sur `GET /professors`, intégré à `appendFilters`/`setParams`/`countSearch`.
- `frontend/src/utils/favorites.ts` : **créé** puis **réécrit en store API-backé authentifié** (plus de localStorage) : lit `cc_token`/`cc_user`, `loadFavoritesIfNeeded`, `toggleFavorite` → `'added'|'removed'|'needs-auth'|'error'`, store versionné + `useSyncExternalStore`, garde l'interface `{isFav, toggle}`.
- `frontend/src/components/ProfesseurCard.tsx` : **refondu** (article, cover gradient + Avatar lg + bouton favori ♥, badge **Profil vérifié**, note « X avis », matière principale, meta niveaux·ville·lieux, prix « À partir de … / heure », CTA Voir le profil).
- `frontend/src/components/ui/Icon.tsx` : icône `heart` ajoutée ; `frontend/src/components/ui/LoadingState.tsx` : `variant="prof"` (skeleton carte, défaut admin inchangé).
- **Expérience confiance + avis (frontend only, voir Completed)** : `frontend/src/pages/FicheProf.tsx` (section `#avis` : RatingSummary « Note moyenne » + liste avis avec auteur/date FR/étoiles/commentaire + état vide « Aucun avis pour le moment. », hero badge « Profil vérifié » — **faux badge « Élève vérifié » supprimé**) ; `frontend/src/components/ui/RatingSummary.tsx` (libellé « Note moyenne ») ; `frontend/src/components/ui/ReviewModal.tsx` (note obligatoire à 0, submit disabled si <1★, `maxLength=2000` + compteur, labels * / optionnel) ; `frontend/src/components/ui/RatingStars.tsx` (label « Sélectionnez une note » pour value=0) ; `frontend/src/pages/student/Dashboard.tsx` (nettoie `reviewError` à l'ouverture, notice 6s auto-dismiss) ; `frontend/src/pages/professor/Dashboard.tsx` (fix étoiles `size={1}`→`size={15}` + date FR) ; `frontend/src/styles/index.css` (`review-empty*`, `review-summary__label`, `review-item__who/stars/no-comment`, `review-step__counter`, `.booking-meeting a` wrap, `review-item__head` wrap).
- `frontend/src/pages/Recherche.tsx` : barre de recherche `results__search` (submit Enter + clear + chip), filtres avancés repliables (auto-open si actifs), tri « Mieux notés… », empty state avec « Effacer les filtres » réel, header « X professeurs trouvés » (totalElements).
- `frontend/src/services/api.ts` : `rejectBooking(id, reason?)` avec `{reason}` ; `createBooking(data: BookingCreateData)` ; `acceptBooking(id, dto?)` avec body ; `markBookingPaid(id)` ; `uploadProfessorPhoto(file)` (PUT brut octet-stream) + `deleteProfessorPhoto()` ; **`getBooking(id)` + `saveMeetingConfig(id, dto)` + `deleteMeetingConfig(id)`**.
- `backend/src/main/java/com/coursconnect/model/enums/PaymentMethod.java`, `.../PaymentStatus.java` : **créés**.
- `backend/src/main/java/com/coursconnect/dto/BookingAcceptDTO.java` : **créé puis réduit à `meetingLocation` seul** (le lien ONLINE a migré vers `saveMeetingConfig`). `dto/MeetingConfigDTO.java` : **créé** (meetingLink ≤500 / meetingPlatform ≤50 / meetingInstructions ≤2000) pour `PUT /bookings/{id}/meeting`.
- `backend/src/main/java/com/coursconnect/model/Booking.java` : + paymentMethod/paymentStatus/amount/currency/paymentReference/paidAt/meetingLocation/meetingLink/meetingPlatform/meetingInstructions.
- `backend/src/main/java/com/coursconnect/model/Professor.java` + `dto/ProfessorProfileDTO.java` + `dto/UpdateProfessorDTO.java` + `service/ProfessorService.java` : + `teachingAddress` + **`profilePhoto`, `updateProfilePhoto`, `clearProfilePhoto`, `toOfferDTO` expose `meetingPlatform`**.
- `backend/src/main/java/com/coursconnect/dto/BookingCreateDTO.java` : + `@NotNull paymentMethod` + `meetingLocation @Size(500)`.
- `backend/src/main/java/com/coursconnect/dto/BookingResponseDTO.java` : + amount/currency/paymentMethod/paymentStatus/meetingLocation/meetingLink/meetingPlatform/meetingInstructions/paymentReference/paidAt + **professorRating/professorReviewCount/professorProfilePhoto/subjectLabel/levelLabel** (helper `firstName(List,fn)` dans `BookingService.toDTO`).
- `backend/src/main/java/com/coursconnect/service/BookingService.java` : create (CASH-only, montant `amountOf`, lieu par LocationType, UNPAID), `accept(BookingAcceptDTO)` (meetingLocation seul), `markPaid` (COMPLETED → PAID+paidAt), **`saveMeetingConfig` (ONLINE + ACCEPTED/COMPLETED + https strict + notif étudiant), `deleteMeetingConfig`, `getParticipantBooking`**, toDTO (lieu/lien/instructions seulement ACCEPTED/COMPLETED).
- `backend/src/main/java/com/coursconnect/rest/BookingResource.java` : accept(DTO null-safe) + `PUT /{id}/payment/mark-paid` + **`GET /{id}` (requireAuth) + `PUT/DELETE /{id}/meeting` (PROFESSOR, `@Valid`)**.
- **Réunion en ligne (backend, modèle lien-par-réservation refonte)** : `model/Offer.java` + `dto/OfferDTO.java` — **plus de `meetingLink`/`meetingInstructions` sur l'offre** (seule `meetingPlatform` 50 reste, publique, `@JsonInclude(NON_NULL)`) ; `service/OfferService.java` (`applyDto`/`toDTO` ignorent link/instructions) ; `util/UrlValidator.isHttpsUrl` (https strict, remplace le lien du schéma snapshot).
- **Dashboard étudiant routé (nouveaux fichiers)** : `frontend/src/components/dashboard/StudentShell.tsx`, `frontend/src/pages/student/Home.tsx`, `StudentBookings.tsx`, `BookingDetail.tsx`, `Profile.tsx`, `Notifications.tsx` ; `frontend/src/App.tsx` (routes `/student*`) ; `frontend/src/pages/student/Dashboard.tsx` **supprimé** ; `frontend/src/pages/FicheProf.tsx` (lien succès → `/student/reservations`). CSS : `.booking-meeting__head`, `.meeting-editor`, `.booking-review-badge`, `.booking-item--skeleton`, `.rating-inline`, `.booking-detail*`, `.dash-hero*`, `.next-course-grid*`, `.notif-list*`, `.dash-card__title` flex-wrap.
- **Photo de profil (backend)** : `rest/UploadResource.java` (PUT/DELETE `/uploads/profiles/me` rôle PROFESSOR, GET public `/uploads/profiles/{fileName}`), `service/PhotoStorageService.java` (stockage `{jboss.server.data.dir}/uploads/profiles`, nom `prof-<id>.<ext>`, lot sûr, mediaType par extension), `util/PhotoValidator.java` (magic bytes JPEG/PNG/WEBP + ≤5 Mo), `util/UrlValidator.java`, `exception/BadRequestException.java` (mappée 400 dans GlobalExceptionMapper, remplace les `IllegalArgumentException` d'EJB → 500 en 400 partout : OfferService/BookingService/PhotoValidator/PhotoStorageService/ReviewService).
- Tests : `PhotoValidatorTest` (8) + `UrlValidatorTest` (**6**) → **23/23 PASS**.
- `backend/repair-utf8.sql`, `backend/ds-fix.cli` : réparation DB mojibake + datasource UTF-8 (appliqués).
- `sql/schema-mvp.sql` : `SET NAMES utf8mb4` + colonnes paiement/lieu (`payment_method`, `payment_status`, `amount`, `currency`, `meeting_location`, `meeting_link`, `teaching_address`...) + **`offers.meeting_platform/meeting_link/meeting_instructions` + `bookings.meeting_platform/meeting_link/meeting_instructions`**.
- Scripts tbd E2E : `%TEMP%\opencode\e2e-booking.ps1`, `e2e-round2.ps1` (resquables pour re-jouer après recréation) ; payloads JSON UTF-8 dans `%TEMP%\opencode\` (`e2e-book-*.json`).
- Vérifs headless avail picker : `%TEMP%\opencode\ui-check\check-booking.js` (7 viewports + flux étudiant + guards no-slot/offre-longue, captures `%TEMP%\opencode\shots\bk-*.png`, `bk-recap.png`) ; probes overflow `probe3/probe5/probe7/probe8.js` ; `check-prof.js` obsolète (picker changé).
- Vérifs headless confiance + avis : `%TEMP%\opencode\ui-check\check-trust.js` (71 checks : overflow 7 viewports prof1, contenu section confiance, empty state prof11/prof9, cartes recherche, formulaire d'avis connecté @1280, overflow dashboard @430/390/360, captures `%TEMP%\opencode\shots\tr-*.png` ; scripts debug `dbg-360-*.js`) ; jamais de réglage sur données : après vérif du flux, delete des reviews de test via `DELETE /admin/reviews/{id}` (admin `admin@coursconnect.fr` / `password123`) pour restaurer la base (prof1 = 2 reviews, avg 5.00).
- **Vérifs headless photo + en ligne : `%TEMP%\opencode\ui-check\check-online.js` (40 checks : API sans fuite lien/instructions, PNG magic bytes servis, photo sur la fiche publique, badge « Zoom » sur fiche, prof1 héros, listes offres « Séance en ligne configurée », édition offre pré-remplie, upload/suppression photo dashboard, ligne accordée étudiante avec bouton « Rejoindre la séance » + instructions, overflow 0 @1440/1280/1024/768/430/390/360 sur fiche prof + dashboard prof + dashboard étudiant, captures `%TEMP%\opencode\shots\on-*.png`)**.
- **Vérif headless lien-par-réservation + dashboard routé : `%TEMP%\opencode\ui-check\check-dash.js` (102 checks, done — voir Work State) ; piège : le lien est stocké dans `href` pas dans le texte → assertions locator `.booking-item:has(a[href*="zoom.us"])` / `a[href*="..."]`**.
- **Dashboard professeur routé + Notifications fiables (nouveaux fichiers)** : `frontend/src/components/dashboard/ProfessorShell.tsx` (sidebar 6 entrées + badge unread), `frontend/src/hooks/useUnreadCount.ts` (polling 90 s), `frontend/src/components/dashboard/BookingCard.tsx` (carte réutilisable), `frontend/src/pages/professor/{Home,ProfessorBookings,ProfessorBookingDetail,ProfessorNotifications,ProfessorProfile,ProfessorOffers,ProfessorOfferForm,ProfessorAvailability}.tsx` ; `frontend/src/App.tsx` (9 routes `/professor*`) ; `frontend/src/pages/professor/Dashboard.tsx` **supprimé** ; `BookingCard`/roles : `flex-wrap:wrap` sur `.booking-item__prof`, `.todo-item*`, `.todo-empty`, `.notif-item__link`/`__cta`.
- **Backend notifications fiables (tâche courante)** : `model/Notification.java` + `dto/NotificationDTO.java` (**referenceId**), `repository/NotificationRepository.java` (`findByIdAndUser`), `service/NotificationService.java` (`notify(..., referenceId)`, `markRead`), `rest/NotificationResource.java` (`PUT /notifications/{id}/read`), `service/BookingService.java` (`cancel` message enrichi `BOOKING_CANCELLED`, **`cancelByProfessor`**, `checkCancellable` 409, `complete` notif étudiant), `rest/BookingResource.java` (`PUT /bookings/{id}/professor-cancel`), `schema-mvp.sql` (`notifications.reference_id`).