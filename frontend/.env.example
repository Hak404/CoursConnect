# Variables publiques uniquement (exposées au navigateur via import.meta.env).
# Aucun secret ne doit être placé ici : DATABASE_URL, JWT_SECRET, SMTP_PASSWORD, etc.
# restent côté serveur/backend.
#
# URL de base de l'API backend en production (HTTPS).
# En local, laisser vide : Vite utilise le proxy de développement (/api -> http://localhost:8081).
VITE_API_URL=